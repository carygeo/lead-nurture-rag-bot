# Required for production-like chat completion responses. If unset, the bot uses a deterministic local fallback response generator.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Optional for OpenAI-compatible gateways.
OPENAI_BASE_URL=
DATA_DIR=data
